#include <iostream>
#include <string>
#include <map>
#include <vector>

using std::map;
using std::vector;
using std::string;
using std::pair;
using std::cin;
using std::cout;
using std::endl;




vector<int> Func (int numbers){

}


/*Дана последовательность натуральных чисел {Aj} (n<=10000) Найти произведение чисел,
 * заканчивающихся цифрой 2 или 4, наименьшее из таких чисел и номер этого числа в последовательности.*/
int main() {

    int a;
    cin >> a;

    vector<int> vec;
    for(int i = 0; i < a; ++i){
        int n;
        cin >> n;
        vec.push_back(n);
    }
    for (int i = 0; i < vec.size(); ++i) {
        if()
    }





}